project_id = "myrma-app"

# Local Postgres for `supabase start` — used by the DB test tier
# (supabase/tests/, .github/workflows/ci.yml "DB · Audit Hardening Tests" job).
# Port matches the CLI's own default; kept explicit for clarity, not required.
[db]
port = 54322

[functions.send-email]
enabled = true
verify_jwt = true
import_map = "./functions/send-email/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/send-email/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/send-email/*.html" ]
